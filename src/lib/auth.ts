// Mock user ID for development
// Replace with actual auth when implementing authentication
export const MOCK_USER_ID = 'mock-user-demo-123'

/**
 * Get mock user ID for server-side operations.
 * This is a placeholder - replace with actual auth when ready.
 */
export function getMockUserId(): string {
  return MOCK_USER_ID
}
